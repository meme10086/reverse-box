# 《Reverse Box：逆时推箱》交付前回归验证报告

- **验证时间**：2026-09-13 13:49（GMT+8）
- **项目路径**：`D:\Game《Reserve box》\ReverseBox_Godot`
- **引擎**：Godot 4.5 stable official（`tools\Godot_v4.5-stable_official_win64.exe`，876b29033）
- **结论**：✅ **游戏可以正常运行，构建包健康，可以交付。**

---

## 1. 关卡逻辑自检

```
<官方4.5> --headless --path <项目> -s res://tests/solve_test.gd
```

**结果：全部 PASS，退出码 0**

| 分组 | 覆盖内容 | 结果 |
|---|---|---|
| R1~R9 | 回溯快照恢复、点数池消耗、耗尽后剩余 0 | PASS |
| I1~I3 | WASD / 方向键动作注册、回溯动作、R 键绑定 | PASS |
| C1~C3 | 箱子推入死角、死锁后推不动、箱子留在原地 | PASS |
| T1~T6 | 教学关提示文案、第 3 关"推上顶行必死 + 按 R 救回" | PASS |

退出时出现的 `CanvasItem RID leaked` / `ObjectDB instances leaked` 为 Godot 无头模式退出时的常规告警，**不影响运行**。

## 2. 主场景冒烟

```
<官方4.5> --headless --path <项目> --quit-after 30
```
启动 `scenes/start_screen.tscn`（选关界面），无任何 error，**EXIT=0**。

## 3. 导出的 Windows 包冒烟

```
build\ReverseBox.exe --headless --quit-after 60
```
**EXIT=0** —— 导出包内的 pck 与引擎均正常。

## 4. Android 包签名校验

| 文件 | 大小 | 签名者 | 结果 |
|---|---|---|---|
| `build/ReverseBox-release.apk` | 76 MB | CN=Reverse Box, O=Bilan, C=CN | ✅ 正式签名，验签通过 |
| `build/ReverseBox.apk` | 84 MB | CN=Android Debug, O=Android, C=US | ✅ 调试签名，验签通过 |

## 5. 构建产物清单（2026-09-10 18:27/18:28）

```
build/ReverseBox.exe            95 MB   Windows 可执行
build/ReverseBox-release.apk    76 MB   Android 正式包
build/ReverseBox.apk            84 MB   Android 调试包
build/screenshots/                      交付截图
build/交付说明.md                        交付文档
```

## 6. 注意事项

- 逻辑自检会写入 `user://progress.cfg`，导致选关界面全部显示"已通关"。**本次已验证并删除** `%APPDATA%\Godot\app_userdata\Reverse Box：逆时推箱\progress.cfg`，当前进度为全新状态。
- 手动试玩：Godot 打开 `project.godot` 按 F5，或直接双击 `build/ReverseBox.exe`。操作：WASD / 方向键移动，R 回溯 1 格。
